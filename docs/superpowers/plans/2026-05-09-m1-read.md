# M1 Read Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** собрать read-only часть продукта — Feed (mobile + desktop), Story page, Reader — на статичных JSON-фикстурах. После M1 пользователь может пролистать ленту, открыть карточку истории, прочитать главу с настройками и прогрессом, без бэка.

**Architecture:** Next.js App Router с route group `(reader)` для main-flow страниц, статичные fixtures в `lib/fixtures/` (типизированы по будущим API-контрактам из spec'ов экранов), tracking — тонкий no-op wrapper `lib/track.ts` (PostHog подключим в M2). Reader settings/progress/idle — три отдельных хука в `lib/reader/`, каждый с одной зоной ответственности. Стриминг и реальные данные — М2, тут текст хардкодится.

**Tech Stack:** Next.js 15 (App Router) · React 19 · TypeScript 5 strict · Tailwind v4 · Vitest + RTL + jsdom · Storybook 10. Все UI-примитивы из M0 (`<BurstSticker>`, `<ScotchTag>`, `<GrainCover>`, `<Marquee>`, `<Ornament>`, `<MonoBadge>`) переиспользуются.

**Длительность:** ~10 рабочих дней.

**Pre-requisites:**

- M0 закрыт (тег `m0-foundation` на `main`, 23/23 теста проходят).
- `pnpm install` выполнен, `pnpm test` зелёный.
- Шрифты подключены в `app/layout.tsx`, токены в `app/globals.css`.

---

## File Structure

```
app/
├── layout.tsx                      already exists (M0)
├── page.tsx                        Task 3 (Feed root, responsive — мобайл + десктоп)
├── (reader)/
│   ├── story/[id]/page.tsx         Task 5
│   └── reader/[storyId]/[chapterN]/page.tsx   Task 7
├── globals.css                     already exists
components/
├── ui/                             already exists (M0 primitives)
├── feed/
│   ├── FeedHeroDesktop.tsx         Task 3
│   ├── FeedHeroMobile.tsx          Task 4
│   ├── FeedGrid.tsx                Task 3 (shared, props: columns)
│   ├── FandomChips.tsx             Task 4
│   ├── FeedHeader.tsx              Task 3 (top bar: logo + nav)
│   ├── TabBarMobile.tsx            Task 4
│   └── FooterDesktop.tsx           Task 3
├── story/
│   ├── StoryHero.tsx               Task 5
│   ├── StoryMetaPanel.tsx          Task 5
│   ├── ChapterListItem.tsx         Task 5
│   └── WatchCtaCard.tsx            Task 5
├── reader/
│   ├── ReaderTopBar.tsx            Task 7
│   ├── ReaderProgressBar.tsx       Task 7
│   ├── ReaderBody.tsx              Task 7 (drop cap + paragraphs + ornaments)
│   ├── ReaderSettingsSheet.tsx     Task 7
│   └── NextChapterCard.tsx         Task 7
lib/
├── prisma.ts                       already exists
├── track.ts                        Task 2 (no-op tracking wrapper)
├── fixtures/
│   ├── stories.ts                  Task 1 (typed mock data)
│   ├── chapters.ts                 Task 1 (hardcoded chapter text для M1-04)
│   └── tropes.ts                   Task 1 (8 tropes для маркизы)
├── types/
│   └── story.ts                    Task 1 (Story, Chapter, FandomChip types)
└── reader/
    ├── useReaderSettings.ts        Task 6
    ├── useReadingProgress.ts       Task 8
    └── useLateNightDim.ts          Task 9
tests/
├── lib/
│   ├── track.test.ts               Task 2
│   └── reader/
│       ├── useReaderSettings.test.ts   Task 6
│       ├── useReadingProgress.test.ts  Task 8
│       └── useLateNightDim.test.ts     Task 9
├── components/
│   ├── feed/
│   │   ├── FeedHeader.test.tsx     Task 3
│   │   ├── FandomChips.test.tsx    Task 4
│   │   └── TabBarMobile.test.tsx   Task 4
│   ├── story/
│   │   ├── ChapterListItem.test.tsx   Task 5
│   │   └── StoryMetaPanel.test.tsx    Task 5
│   └── reader/
│       ├── ReaderProgressBar.test.tsx  Task 7
│       └── ReaderSettingsSheet.test.tsx Task 7
└── pages/
    ├── feed.test.tsx               Task 3-4 (smoke + key elements)
    ├── story.test.tsx              Task 5
    └── reader.test.tsx              Task 7
stories/
└── ui/                             already exists (M0)
└── feed/
    ├── FandomChips.stories.tsx     Task 11
    ├── FeedHeroDesktop.stories.tsx Task 11
    └── TabBarMobile.stories.tsx    Task 11
└── story/
    ├── ChapterListItem.stories.tsx Task 11
    └── WatchCtaCard.stories.tsx    Task 11
└── reader/
    └── ReaderSettingsSheet.stories.tsx  Task 11
```

**Принципы декомпозиции:**

- Каждая «зона» страницы — компонент в `components/{feed|story|reader}/`. Page-файлы (`page.tsx`) тонкие — только композиция.
- Хуки читалки — три отдельных файла, по одной зоне ответственности. Тестируются изолированно от UI.
- Fixtures отделены от типов — типы в `lib/types/story.ts` совпадают с будущими API-контрактами (см. screens-спеки), фикстуры импортируют типы.

---

## Task 1: Fixtures и типы данных

**Files:**

- Create: `lib/types/story.ts`
- Create: `lib/fixtures/stories.ts`
- Create: `lib/fixtures/chapters.ts`
- Create: `lib/fixtures/tropes.ts`
- Test: tests use these directly — отдельных тестов на фикстуры нет (это статика).

- [ ] **Step 1: Создать типы**

```ts
// lib/types/story.ts
export type FandomColor = { color1: string; color2: string };

export type Fandom = {
  id: string;
  name: string;
  slug: string;
  color1: string;
  color2: string;
};

export type FandomChip = {
  id: string;
  name: string;
  sub?: string;
  active?: boolean;
};

export type StoryAuthor = { id: string; handle: string };

export type Story = {
  id: string;
  title: string;
  fandom: Fandom;
  pair: string;
  tagline: string;
  cover: string | null;
  likes: number;
  chapters: number;
  hasWatch: boolean;
  watchEpisodes?: number;
  author: StoryAuthor;
};

export type ChapterState = 'read' | 'reading' | 'unread' | 'locked' | 'generating';

export type Chapter = {
  n: number;
  title: string;
  minutes: number;
  state: ChapterState;
};

export type ReadingProgress = {
  lastChapterN: number;
  lastParagraph: number;
};

export type StoryDetail = {
  story: Story;
  chapters: Chapter[];
  progress: ReadingProgress | null;
  watchAvailable: boolean;
  saved: boolean;
};

export type ChapterContent = {
  storyId: string;
  n: number;
  title: string;
  paragraphs: string[];
};
```

- [ ] **Step 2: Создать fixtures для историй**

```ts
// lib/fixtures/stories.ts
import type { Story, Fandom, FandomChip } from '@/lib/types/story';

const aftg: Fandom = {
  id: 'aftg',
  name: 'All for the Game',
  slug: 'all-for-the-game',
  color1: '#1a0f0a',
  color2: '#2d1810',
};

const hp: Fandom = {
  id: 'hp',
  name: 'Harry Potter',
  slug: 'harry-potter',
  color1: '#0e1a2b',
  color2: '#1a2438',
};

const naruto: Fandom = {
  id: 'naruto',
  name: 'Naruto',
  slug: 'naruto',
  color1: '#2b0e0e',
  color2: '#3a1818',
};

const jjk: Fandom = {
  id: 'jjk',
  name: 'JJK',
  slug: 'jjk',
  color1: '#1a1a2e',
  color2: '#16213e',
};

export const fandomChips: FandomChip[] = [
  { id: 'all', name: 'все', active: true },
  { id: 'aftg', name: 'AFTG', sub: 'foxhole court' },
  { id: 'hp', name: 'HP', sub: 'дрэмиона' },
  { id: 'naruto', name: 'naruto', sub: 'sasunaru' },
  { id: 'jjk', name: 'JJK', sub: 'satosugu' },
];

export const heroStory: Story = {
  id: 'hero-1',
  title: 'кто остаётся в полночь',
  fandom: aftg,
  pair: 'Эндрю × Нил',
  tagline: 'когда лисы спят, на крыше остаются только двое',
  cover: null,
  likes: 24800,
  chapters: 14,
  hasWatch: true,
  watchEpisodes: 4,
  author: { id: 'u1', handle: 'palmetto' },
};

export const feedStories: Story[] = [
  {
    id: 's1',
    title: 'дешифровка',
    fandom: hp,
    pair: 'Драко × Гермиона',
    tagline: 'библиотека пуста, кроме них двоих',
    cover: null,
    likes: 12300,
    chapters: 9,
    hasWatch: false,
    author: { id: 'u2', handle: 'parchment' },
  },
  {
    id: 's2',
    title: 'дозор',
    fandom: naruto,
    pair: 'Саске × Наруто',
    tagline: 'третий час ночи на границе',
    cover: null,
    likes: 8700,
    chapters: 6,
    hasWatch: true,
    watchEpisodes: 2,
    author: { id: 'u3', handle: 'shinobi' },
  },
  {
    id: 's3',
    title: 'без проклятий',
    fandom: jjk,
    pair: 'Годжо × Гето',
    tagline: 'если бы они остались в академии',
    cover: null,
    likes: 15600,
    chapters: 11,
    hasWatch: true,
    watchEpisodes: 3,
    author: { id: 'u4', handle: 'limitless' },
  },
  {
    id: 's4',
    title: 'правила игры',
    fandom: aftg,
    pair: 'Кевин × Жан',
    tagline: 'старые шрамы новые партнёры',
    cover: null,
    likes: 5400,
    chapters: 4,
    hasWatch: false,
    author: { id: 'u5', handle: 'queen' },
  },
  {
    id: 's5',
    title: 'непрочитанное',
    fandom: hp,
    pair: 'Гарри × Драко',
    tagline: 'переписка восьмого курса',
    cover: null,
    likes: 9200,
    chapters: 7,
    hasWatch: false,
    author: { id: 'u6', handle: 'eighthyear' },
  },
];

export function getStoryById(id: string): Story | undefined {
  return [heroStory, ...feedStories].find((s) => s.id === id);
}
```

- [ ] **Step 3: Создать fixtures для тропов**

```ts
// lib/fixtures/tropes.ts
export const trendingTropes = [
  'enemies-to-lovers',
  'slow burn',
  'time travel',
  'soulmates',
  'fake dating',
  'touch starved',
  'only one bed',
  'amnesia',
];
```

- [ ] **Step 4: Создать fixtures для глав (включая полный текст ch.7)**

```ts
// lib/fixtures/chapters.ts
import type { Chapter, ChapterContent, StoryDetail, ReadingProgress } from '@/lib/types/story';
import { heroStory, getStoryById } from './stories';

const heroChapters: Chapter[] = [
  { n: 1, title: 'первый матч', minutes: 4, state: 'read' },
  { n: 2, title: 'разговор на крыше', minutes: 5, state: 'read' },
  { n: 3, title: 'после игры', minutes: 4, state: 'read' },
  { n: 4, title: 'тишина в раздевалке', minutes: 6, state: 'read' },
  { n: 5, title: 'дорога домой', minutes: 4, state: 'read' },
  { n: 6, title: 'между сменами', minutes: 5, state: 'read' },
  { n: 7, title: 'полночное чтиво', minutes: 4, state: 'reading' },
  { n: 8, title: 'утро воскресенья', minutes: 5, state: 'unread' },
  { n: 9, title: 'разговор с Беттси', minutes: 6, state: 'unread' },
  { n: 10, title: 'тренировка вне расписания', minutes: 4, state: 'unread' },
  { n: 11, title: 'команда против команды', minutes: 7, state: 'unread' },
  { n: 12, title: 'звонок в три ночи', minutes: 5, state: 'unread' },
  { n: 13, title: 'возвращение', minutes: 6, state: 'unread' },
  { n: 14, title: 'финал сезона', minutes: 8, state: 'locked' },
];

export function getStoryDetail(id: string): StoryDetail | null {
  const story = getStoryById(id);
  if (!story) return null;
  if (story.id !== heroStory.id) {
    return {
      story,
      chapters: Array.from({ length: story.chapters }, (_, i) => ({
        n: i + 1,
        title: `глава ${i + 1}`,
        minutes: 5,
        state: i === 0 ? 'unread' : 'unread',
      })),
      progress: null,
      watchAvailable: story.hasWatch,
      saved: false,
    };
  }
  return {
    story,
    chapters: heroChapters,
    progress: { lastChapterN: 7, lastParagraph: 0 },
    watchAvailable: true,
    saved: true,
  };
}

const ch7: ChapterContent = {
  storyId: heroStory.id,
  n: 7,
  title: 'полночное чтиво',
  paragraphs: [
    'Крыша всё ещё держала тепло прошедшего дня, и Нил подтянул колени к груди, упираясь подбородком в ткань тренировочной футболки. Огни Палметто внизу мигали ровно, как метроном, по которому можно было засыпать, если бы вообще получалось.',
    'Эндрю сидел рядом, в полуметре, вытянув ноги. Сигарета в его пальцах уже почти догорела, но он не торопился её гасить. Он молчал — но это было то самое его молчание, которое не значило отсутствие.',
    'Минут пять назад Нил спросил, что он читает. Эндрю ответил «ничего», и этого хватило.',
    '— Я не понимаю, почему ты остаёшься, — наконец сказал Нил, не глядя на него.',
    '— Сто процентов, — отозвался Эндрю. — Это и не должно быть понятно.',
    'Нил повернул голову. В свете далёкой неоновой вывески над парковкой ресницы Эндрю отбрасывали короткую тень. Он выглядел уставшим — не больше обычного, но иначе. Как человек, который долго что-то решал и теперь принял.',
    '— Ты злишься?',
    '— Нет.',
    '— Я не верю.',
    'Эндрю наконец посмотрел на него — спокойно, без вызова. — Тогда не верь.',
    'Сигарета погасла сама. Эндрю стряхнул пепел в банку, которую кто-то из лис оставил здесь ещё в сентябре. Нил всё ещё смотрел на него и думал, что у крыши есть одно странное свойство: на ней всё, что он говорил вслух, звучало честнее, чем внизу.',
    '— Кевин думает, ты собираешься ехать на тренировочный лагерь раньше команды, — сказал Нил.',
    '— Кевин много думает.',
    '— И что я должен ему сказать?',
    'Эндрю пожал плечами. — Скажи ему правду. Я не еду никуда без тебя.',
  ],
};

export function getChapterContent(storyId: string, n: number): ChapterContent | null {
  if (storyId === heroStory.id && n === 7) return ch7;
  return null;
}

export const heroProgress: ReadingProgress = { lastChapterN: 7, lastParagraph: 0 };
```

- [ ] **Step 5: Запустить typecheck**

Run: `pnpm typecheck`
Expected: PASS (no errors).

- [ ] **Step 6: Commit**

```bash
git add lib/types/ lib/fixtures/
git commit -m "feat(M1-data): static fixtures + typed contracts for feed/story/reader"
```

---

## Task 2: Tracking wrapper (no-op)

**Files:**

- Create: `lib/track.ts`
- Test: `tests/lib/track.test.ts`

**Контекст:** PostHog/Plausible подключим в M2 (нужен бэк/env-key). Сейчас — типизированный no-op, чтобы экраны уже вызывали правильно. На dev пишем в console, в prod молчим.

- [ ] **Step 1: Написать падающий тест**

```ts
// tests/lib/track.test.ts
// @vitest-environment jsdom
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
import { track } from '@/lib/track';

describe('track', () => {
  let logSpy: ReturnType<typeof vi.spyOn>;

  beforeEach(() => {
    logSpy = vi.spyOn(console, 'debug').mockImplementation(() => {});
  });

  afterEach(() => {
    logSpy.mockRestore();
  });

  it('logs event name and props in dev', () => {
    track('feed_viewed', { source: 'home' });
    expect(logSpy).toHaveBeenCalledWith('[track]', 'feed_viewed', { source: 'home' });
  });

  it('accepts no props', () => {
    track('feed_pull_refresh');
    expect(logSpy).toHaveBeenCalledWith('[track]', 'feed_pull_refresh', undefined);
  });
});
```

- [ ] **Step 2: Запустить тест — должен упасть**

Run: `pnpm test tests/lib/track.test.ts`
Expected: FAIL — `Cannot find module '@/lib/track'`.

- [ ] **Step 3: Написать минимальную реализацию**

```ts
// lib/track.ts
export type TrackEventName =
  | 'feed_viewed'
  | 'feed_hero_tap'
  | 'feed_chip_tap'
  | 'feed_card_tap'
  | 'feed_watch_chip_tap'
  | 'feed_pull_refresh'
  | 'story_viewed'
  | 'story_save_toggle'
  | 'story_share'
  | 'story_chapter_tap'
  | 'story_continue_tap'
  | 'story_watch_tap'
  | 'reader_opened'
  | 'reader_progress_milestone'
  | 'reader_settings_changed'
  | 'reader_watch_chip_tap'
  | 'reader_chapter_completed'
  | 'reader_stream_error';

export function track(name: TrackEventName, props?: Record<string, unknown>): void {
  // M2: подключим PostHog. Пока no-op + dev-debug.
  if (process.env.NODE_ENV !== 'production') {
    console.debug('[track]', name, props);
  }
}
```

- [ ] **Step 4: Запустить тест — должен пройти**

Run: `pnpm test tests/lib/track.test.ts`
Expected: PASS (2 tests).

- [ ] **Step 5: Commit**

```bash
git add lib/track.ts tests/lib/track.test.ts
git commit -m "feat(M1-05): typed track() no-op wrapper for tracking events"
```

---

## Task 3: Feed Desktop screen (M1-01)

**Files:**

- Create: `app/page.tsx` (replaces M0 placeholder)
- Create: `components/feed/FeedHeader.tsx`
- Create: `components/feed/FeedHeroDesktop.tsx`
- Create: `components/feed/FeedGrid.tsx`
- Create: `components/feed/FooterDesktop.tsx`
- Test: `tests/components/feed/FeedHeader.test.tsx`
- Test: `tests/pages/feed.test.tsx` (smoke)

**Контекст:** Desktop — secondary target по spec'у `06-feed-desktop.md`. Mobile-версию делаем в Task 4 — на этом этапе page.tsx делает только Desktop layout, mobile добавим как responsive-вариацию. Маркиза трендов — переиспользуем `<Marquee>` из M0.

- [ ] **Step 1: Написать падающий тест на FeedHeader**

```tsx
// tests/components/feed/FeedHeader.test.tsx
// @vitest-environment jsdom
import { describe, it, expect } from 'vitest';
import { render, screen } from '@testing-library/react';
import { FeedHeader } from '@/components/feed/FeedHeader';

describe('FeedHeader', () => {
  it('renders wordmark and primary nav links', () => {
    render(<FeedHeader />);
    expect(screen.getByText(/head/i)).toBeInTheDocument();
    expect(screen.getByText(/canon/i)).toBeInTheDocument();
    expect(screen.getByRole('link', { name: /feed/i })).toBeInTheDocument();
    expect(screen.getByRole('link', { name: /fandoms/i })).toBeInTheDocument();
  });

  it('renders + NEW CTA when authed prop is true', () => {
    render(<FeedHeader authed />);
    expect(screen.getByRole('button', { name: /new/i })).toBeInTheDocument();
  });

  it('renders ВОЙТИ when authed false', () => {
    render(<FeedHeader authed={false} />);
    expect(screen.getByRole('button', { name: /войти/i })).toBeInTheDocument();
  });
});
```

- [ ] **Step 2: Запустить тест — должен упасть**

Run: `pnpm test tests/components/feed/FeedHeader.test.tsx`
Expected: FAIL — module not found.

- [ ] **Step 3: Реализовать FeedHeader**

```tsx
// components/feed/FeedHeader.tsx
'use client';

import Link from 'next/link';

export function FeedHeader({ authed = false }: { authed?: boolean }) {
  return (
    <header className="hidden lg:flex sticky top-0 z-30 items-center justify-between gap-6 border-b border-ink-faint/20 bg-bg-deep/95 px-8 py-4 backdrop-blur">
      <Link href="/" className="font-display text-2xl">
        head<span className="italic text-amber-default">canon</span>
      </Link>

      <nav className="flex gap-6 font-mono text-xs uppercase tracking-wide">
        <Link href="/" className="text-amber-default">
          feed
        </Link>
        <Link href="/fandoms" className="text-ink-dim hover:text-ink-default">
          fandoms
        </Link>
        <Link href="/tropes" className="text-ink-dim hover:text-ink-default">
          tropes
        </Link>
        <Link href="/watch" className="text-ink-dim hover:text-ink-default">
          watch ▸
        </Link>
      </nav>

      <div className="flex items-center gap-3">
        <input
          type="search"
          placeholder="искать..."
          className="rounded-full border border-ink-faint/30 bg-transparent px-4 py-1.5 font-body text-sm placeholder:text-ink-dim"
        />
        <button
          type="button"
          className="rounded-full bg-amber-default px-4 py-1.5 font-mono text-xs uppercase tracking-wider text-bg-deep"
        >
          {authed ? '+ new ☆' : '★ войти'}
        </button>
      </div>
    </header>
  );
}
```

- [ ] **Step 4: Запустить тест — должен пройти**

Run: `pnpm test tests/components/feed/FeedHeader.test.tsx`
Expected: PASS (3 tests).

- [ ] **Step 5: Реализовать FeedHeroDesktop**

```tsx
// components/feed/FeedHeroDesktop.tsx
import { GrainCover } from '@/components/ui/GrainCover';
import { BurstSticker } from '@/components/ui/BurstSticker';
import { ScotchTag } from '@/components/ui/ScotchTag';
import { MonoBadge } from '@/components/ui/MonoBadge';
import type { Story } from '@/lib/types/story';

export function FeedHeroDesktop({ story }: { story: Story }) {
  return (
    <section className="grid grid-cols-[1.1fr_1fr] items-center gap-12 px-8 py-12">
      <div className="flex flex-col gap-5">
        <MonoBadge variant="amber">
          vol.1 · {story.fandom.name.toLowerCase()} · {story.chapters} ch
        </MonoBadge>
        <h1 className="font-display text-7xl leading-tight text-balance">
          кто остаётся <span className="italic text-amber-default">в полночь</span>
        </h1>
        <p className="font-body text-base text-ink-dim italic">★ {story.tagline} ★</p>
        <div className="flex gap-4">
          <button
            type="button"
            className="rounded-full bg-amber-default px-6 py-3 font-mono text-sm uppercase tracking-wider text-bg-deep"
          >
            ★ читать
          </button>
          {story.hasWatch ? (
            <button
              type="button"
              className="rounded-full border border-chrome-default px-6 py-3 font-mono text-sm uppercase tracking-wider text-chrome-default"
            >
              ▸ watch · {story.watchEpisodes} ep
            </button>
          ) : null}
        </div>
        <div className="flex gap-4 font-mono text-xs uppercase tracking-wide text-ink-dim">
          <span>♡ {(story.likes / 1000).toFixed(1)}k</span>
          <span>@{story.author.handle}</span>
          <span>{story.chapters} chapters</span>
        </div>
      </div>

      <div className="relative aspect-[4/3] -rotate-[1.5deg]">
        <GrainCover
          color1={story.fandom.color1}
          color2={story.fandom.color2}
          className="h-full w-full"
        >
          <ScotchTag className="absolute left-4 top-4">{story.fandom.name}</ScotchTag>
          <ScotchTag className="absolute right-6 top-8 rotate-3">{story.pair}</ScotchTag>
          <BurstSticker className="absolute -right-4 -top-4 rotate-[-8deg]">★ хит</BurstSticker>
        </GrainCover>
      </div>
    </section>
  );
}
```

- [ ] **Step 6: Реализовать FeedGrid**

```tsx
// components/feed/FeedGrid.tsx
'use client';

import Link from 'next/link';
import { GrainCover } from '@/components/ui/GrainCover';
import { ScotchTag } from '@/components/ui/ScotchTag';
import { track } from '@/lib/track';
import type { Story } from '@/lib/types/story';

type Props = { stories: Story[]; columns: 2 | 4 | 5 };

export function FeedGrid({ stories, columns }: Props) {
  const colsClass = columns === 5 ? 'grid-cols-5' : columns === 4 ? 'grid-cols-4' : 'grid-cols-2';
  return (
    <div className={`grid ${colsClass} gap-6 px-8`}>
      {stories.map((s, idx) => (
        <Link
          key={s.id}
          href={`/story/${s.id}`}
          onClick={() => track('feed_card_tap', { story_id: s.id, position: idx })}
          className="group flex flex-col gap-3 transition-transform hover:scale-[1.02]"
        >
          <div className="relative aspect-[2/3]">
            <GrainCover color1={s.fandom.color1} color2={s.fandom.color2} className="h-full w-full">
              <ScotchTag className="absolute left-3 top-3">{s.fandom.name}</ScotchTag>
            </GrainCover>
          </div>
          <h3 className="font-display text-sm leading-snug">{s.title}</h3>
          <span className="font-mono text-[10px] uppercase tracking-wide text-ink-dim">
            ♡ {(s.likes / 1000).toFixed(1)}k · ch {s.chapters}
          </span>
        </Link>
      ))}
    </div>
  );
}
```

- [ ] **Step 7: Реализовать FooterDesktop**

```tsx
// components/feed/FooterDesktop.tsx
export function FooterDesktop() {
  return (
    <footer className="hidden lg:flex items-center justify-between border-t border-ink-faint/20 px-8 py-4 font-mono text-[10px] uppercase tracking-wider text-ink-dim">
      <span>© 2026 headcanon</span>
      <span>telegram · policies</span>
    </footer>
  );
}
```

- [ ] **Step 8: Прописать `app/page.tsx` (только desktop пока)**

```tsx
// app/page.tsx
import { FeedHeader } from '@/components/feed/FeedHeader';
import { FeedHeroDesktop } from '@/components/feed/FeedHeroDesktop';
import { FeedGrid } from '@/components/feed/FeedGrid';
import { FooterDesktop } from '@/components/feed/FooterDesktop';
import { Marquee } from '@/components/ui/Marquee';
import { Ornament } from '@/components/ui/Ornament';
import { heroStory, feedStories } from '@/lib/fixtures/stories';
import { trendingTropes } from '@/lib/fixtures/tropes';

export default function FeedPage() {
  const ticker = trendingTropes.map((t) => `★ ${t.toUpperCase()}`).join('  ');
  return (
    <div className="min-h-screen bg-bg-deep text-ink-default">
      <FeedHeader />
      <Marquee className="hidden lg:block bg-amber-default text-bg-deep">{ticker}</Marquee>
      <FeedHeroDesktop story={heroStory} />
      <Ornament className="hidden lg:block py-8">★ полночное чтиво для тех, кто не спит ★</Ornament>
      <FeedGrid stories={feedStories} columns={5} />
      <FooterDesktop />
    </div>
  );
}
```

- [ ] **Step 9: Smoke-тест страницы**

```tsx
// tests/pages/feed.test.tsx
// @vitest-environment jsdom
import { describe, it, expect } from 'vitest';
import { render, screen } from '@testing-library/react';
import FeedPage from '@/app/page';

describe('Feed page', () => {
  it('renders hero title and at least 5 grid cards', () => {
    render(<FeedPage />);
    expect(screen.getByRole('heading', { level: 1 })).toBeInTheDocument();
    const cards = screen
      .getAllByRole('link')
      .filter((a) => a.getAttribute('href')?.startsWith('/story/'));
    expect(cards.length).toBeGreaterThanOrEqual(5);
  });
});
```

- [ ] **Step 10: Запустить все тесты + typecheck + lint**

Run: `pnpm test && pnpm typecheck && pnpm lint`
Expected: all PASS.

- [ ] **Step 11: Commit**

```bash
git add app/page.tsx components/feed/ tests/components/feed/ tests/pages/
git commit -m "feat(M1-01): feed desktop screen with hero, marquee, 5-col grid"
```

---

## Task 4: Feed Mobile + responsive (M1-02)

**Files:**

- Create: `components/feed/FeedHeroMobile.tsx`
- Create: `components/feed/FandomChips.tsx`
- Create: `components/feed/TabBarMobile.tsx`
- Create: `components/feed/FeedHeaderMobile.tsx`
- Modify: `app/page.tsx` — добавить мобильную версию через responsive classes
- Test: `tests/components/feed/FandomChips.test.tsx`
- Test: `tests/components/feed/TabBarMobile.test.tsx`

- [ ] **Step 1: Написать падающий тест на FandomChips**

```tsx
// tests/components/feed/FandomChips.test.tsx
// @vitest-environment jsdom
import { describe, it, expect, vi } from 'vitest';
import { render, screen, fireEvent } from '@testing-library/react';
import { FandomChips } from '@/components/feed/FandomChips';
import { fandomChips } from '@/lib/fixtures/stories';

describe('FandomChips', () => {
  it('renders all chips and marks active one', () => {
    render(<FandomChips chips={fandomChips} onSelect={() => {}} />);
    expect(screen.getByText(/все/i)).toBeInTheDocument();
    expect(screen.getByText(/AFTG/i)).toBeInTheDocument();
  });

  it('fires onSelect with chip id when clicked', () => {
    const onSelect = vi.fn();
    render(<FandomChips chips={fandomChips} onSelect={onSelect} />);
    fireEvent.click(screen.getByText(/AFTG/i));
    expect(onSelect).toHaveBeenCalledWith('aftg');
  });
});
```

- [ ] **Step 2: Запустить тест — должен упасть**

Run: `pnpm test tests/components/feed/FandomChips.test.tsx`
Expected: FAIL — module not found.

- [ ] **Step 3: Реализовать FandomChips**

```tsx
// components/feed/FandomChips.tsx
'use client';

import { track } from '@/lib/track';
import type { FandomChip } from '@/lib/types/story';

type Props = {
  chips: FandomChip[];
  onSelect: (id: string) => void;
};

export function FandomChips({ chips, onSelect }: Props) {
  return (
    <div
      className="flex gap-2 overflow-x-auto px-4 py-3 [scroll-snap-type:x_mandatory]"
      role="tablist"
    >
      {chips.map((c) => (
        <button
          key={c.id}
          type="button"
          role="tab"
          aria-selected={c.active ? true : false}
          onClick={() => {
            onSelect(c.id);
            track('feed_chip_tap', { fandom_id: c.id });
          }}
          className={`shrink-0 snap-start rounded-full border px-4 py-1.5 font-mono text-xs uppercase tracking-wide transition-colors ${
            c.active
              ? 'border-amber-default bg-amber-default text-bg-deep'
              : 'border-ink-faint/30 text-ink-default hover:border-amber-default/50'
          }`}
        >
          {c.active ? '★ ' : ''}
          {c.name}
          {c.sub ? <span className="ml-1 text-[10px] opacity-60">{c.sub}</span> : null}
        </button>
      ))}
    </div>
  );
}
```

- [ ] **Step 4: Запустить тест**

Run: `pnpm test tests/components/feed/FandomChips.test.tsx`
Expected: PASS (2 tests).

- [ ] **Step 5: Написать падающий тест на TabBarMobile**

```tsx
// tests/components/feed/TabBarMobile.test.tsx
// @vitest-environment jsdom
import { describe, it, expect } from 'vitest';
import { render, screen } from '@testing-library/react';
import { TabBarMobile } from '@/components/feed/TabBarMobile';

describe('TabBarMobile', () => {
  it('renders 4 tabs', () => {
    render(<TabBarMobile active="feed" />);
    expect(screen.getByRole('link', { name: /лента/i })).toBeInTheDocument();
    expect(screen.getByRole('link', { name: /создать/i })).toBeInTheDocument();
    expect(screen.getByRole('link', { name: /полка/i })).toBeInTheDocument();
    expect(screen.getByRole('link', { name: /я/i })).toBeInTheDocument();
  });

  it('marks active tab', () => {
    render(<TabBarMobile active="saved" />);
    expect(screen.getByRole('link', { name: /полка/i })).toHaveAttribute('aria-current', 'page');
  });
});
```

- [ ] **Step 6: Запустить тест — должен упасть**

Run: `pnpm test tests/components/feed/TabBarMobile.test.tsx`
Expected: FAIL.

- [ ] **Step 7: Реализовать TabBarMobile**

```tsx
// components/feed/TabBarMobile.tsx
import Link from 'next/link';

type TabKey = 'feed' | 'create' | 'saved' | 'me';

const tabs: Array<{ key: TabKey; label: string; href: string }> = [
  { key: 'feed', label: 'лента', href: '/' },
  { key: 'create', label: 'создать', href: '/create' },
  { key: 'saved', label: 'полка', href: '/me/saved' },
  { key: 'me', label: 'я', href: '/me' },
];

export function TabBarMobile({ active }: { active: TabKey }) {
  return (
    <nav className="lg:hidden fixed bottom-0 left-0 right-0 z-30 grid grid-cols-4 border-t border-ink-faint/20 bg-bg-deep/95 backdrop-blur">
      {tabs.map((t) => (
        <Link
          key={t.key}
          href={t.href}
          aria-current={t.key === active ? 'page' : undefined}
          className={`flex items-center justify-center py-3 font-mono text-xs uppercase tracking-wider ${
            t.key === active ? 'text-amber-default' : 'text-ink-dim'
          }`}
        >
          {t.label}
        </Link>
      ))}
    </nav>
  );
}
```

- [ ] **Step 8: Запустить тест**

Run: `pnpm test tests/components/feed/TabBarMobile.test.tsx`
Expected: PASS.

- [ ] **Step 9: Реализовать FeedHeaderMobile**

```tsx
// components/feed/FeedHeaderMobile.tsx
import Link from 'next/link';

export function FeedHeaderMobile() {
  return (
    <header className="lg:hidden flex items-center justify-between px-4 py-3">
      <Link href="/" className="font-display text-xl">
        head<span className="italic text-amber-default">canon</span>
      </Link>
      <div className="flex gap-3 font-mono text-base">
        <button type="button" aria-label="search">
          🔍
        </button>
        <button type="button" aria-label="saved">
          ♡
        </button>
      </div>
    </header>
  );
}
```

- [ ] **Step 10: Реализовать FeedHeroMobile**

```tsx
// components/feed/FeedHeroMobile.tsx
import { GrainCover } from '@/components/ui/GrainCover';
import { BurstSticker } from '@/components/ui/BurstSticker';
import { MonoBadge } from '@/components/ui/MonoBadge';
import { Ornament } from '@/components/ui/Ornament';
import type { Story } from '@/lib/types/story';

export function FeedHeroMobile({ story }: { story: Story }) {
  return (
    <section className="px-4">
      <div className="relative aspect-[4/3]">
        <GrainCover
          color1={story.fandom.color1}
          color2={story.fandom.color2}
          className="h-full w-full"
        >
          <BurstSticker className="absolute right-3 top-3 rotate-[-8deg]">★ хит</BurstSticker>
          <div className="absolute inset-x-0 bottom-0 bg-bg-deep/70 px-3 py-2 backdrop-blur">
            <div className="flex items-center justify-between font-mono text-[10px] uppercase tracking-wide">
              <span>
                {story.fandom.name} · {story.pair}
              </span>
              {story.hasWatch ? (
                <span className="rounded-full border border-chrome-default px-2 py-0.5 text-chrome-default">
                  ▸ watch · {story.watchEpisodes} эп
                </span>
              ) : null}
            </div>
          </div>
        </GrainCover>
      </div>
      <Ornament className="py-4 text-sm">★ полночное чтиво для тех, кто не спит ★</Ornament>
    </section>
  );
}
```

- [ ] **Step 11: Обновить app/page.tsx** — single page, responsive, mobile + desktop

```tsx
// app/page.tsx
'use client';

import { FeedHeader } from '@/components/feed/FeedHeader';
import { FeedHeaderMobile } from '@/components/feed/FeedHeaderMobile';
import { FeedHeroDesktop } from '@/components/feed/FeedHeroDesktop';
import { FeedHeroMobile } from '@/components/feed/FeedHeroMobile';
import { FeedGrid } from '@/components/feed/FeedGrid';
import { FooterDesktop } from '@/components/feed/FooterDesktop';
import { TabBarMobile } from '@/components/feed/TabBarMobile';
import { FandomChips } from '@/components/feed/FandomChips';
import { Marquee } from '@/components/ui/Marquee';
import { Ornament } from '@/components/ui/Ornament';
import { heroStory, feedStories, fandomChips } from '@/lib/fixtures/stories';
import { trendingTropes } from '@/lib/fixtures/tropes';
import { useEffect } from 'react';
import { track } from '@/lib/track';

export default function FeedPage() {
  const ticker = trendingTropes.map((t) => `★ ${t.toUpperCase()}`).join('  ');

  useEffect(() => {
    track('feed_viewed');
  }, []);

  return (
    <div className="min-h-screen bg-bg-deep text-ink-default pb-16 lg:pb-0">
      <FeedHeader />
      <FeedHeaderMobile />
      <Marquee className="bg-amber-default text-bg-deep text-[11px] lg:text-sm">{ticker}</Marquee>
      <div className="lg:hidden">
        <FandomChips chips={fandomChips} onSelect={() => {}} />
      </div>
      <div className="hidden lg:block">
        <FeedHeroDesktop story={heroStory} />
      </div>
      <div className="lg:hidden">
        <FeedHeroMobile story={heroStory} />
      </div>
      <Ornament className="hidden lg:block py-8">★ полночное чтиво для тех, кто не спит ★</Ornament>
      <FeedGrid stories={feedStories} columns={5} />
      <FooterDesktop />
      <TabBarMobile active="feed" />
    </div>
  );
}
```

Note: `FeedGrid` columns prop = 5 на desktop. На mobile нужны 2 колонки. Скорректировать FeedGrid: добавить prop `mobileColumns: 2 | 4` и использовать responsive классы.

- [ ] **Step 12: Обновить FeedGrid для responsive колонок**

```tsx
// components/feed/FeedGrid.tsx — заменить целиком
'use client';

import Link from 'next/link';
import { GrainCover } from '@/components/ui/GrainCover';
import { ScotchTag } from '@/components/ui/ScotchTag';
import { track } from '@/lib/track';
import type { Story } from '@/lib/types/story';

type Props = { stories: Story[] };

export function FeedGrid({ stories }: Props) {
  return (
    <div className="grid grid-cols-2 gap-3 px-4 lg:grid-cols-5 lg:gap-6 lg:px-8">
      {stories.map((s, idx) => (
        <Link
          key={s.id}
          href={`/story/${s.id}`}
          onClick={() => track('feed_card_tap', { story_id: s.id, position: idx })}
          className="group flex flex-col gap-2 transition-transform hover:scale-[1.02] lg:gap-3"
        >
          <div className="relative aspect-[2/3]">
            <GrainCover color1={s.fandom.color1} color2={s.fandom.color2} className="h-full w-full">
              <ScotchTag className="absolute left-3 top-3">{s.fandom.name}</ScotchTag>
            </GrainCover>
          </div>
          <h3 className="font-display text-sm leading-snug lg:text-sm">{s.title}</h3>
          <span className="font-mono text-[10px] uppercase tracking-wide text-ink-dim">
            ♡ {(s.likes / 1000).toFixed(1)}k · ch {s.chapters}
          </span>
        </Link>
      ))}
    </div>
  );
}
```

И в `app/page.tsx` убрать prop `columns={5}` из вызова `<FeedGrid stories={feedStories} />`.

- [ ] **Step 13: Запустить все тесты + typecheck + lint**

Run: `pnpm test && pnpm typecheck && pnpm lint`
Expected: all PASS.

- [ ] **Step 14: Commit**

```bash
git add components/feed/ app/page.tsx tests/components/feed/
git commit -m "feat(M1-02): feed mobile (header/hero/chips/tab bar) + responsive grid"
```

---

## Task 5: Story page (M1-03)

**Files:**

- Create: `app/(reader)/story/[id]/page.tsx`
- Create: `components/story/StoryHero.tsx`
- Create: `components/story/StoryMetaPanel.tsx`
- Create: `components/story/WatchCtaCard.tsx`
- Create: `components/story/ChapterListItem.tsx`
- Test: `tests/components/story/ChapterListItem.test.tsx`
- Test: `tests/components/story/StoryMetaPanel.test.tsx`
- Test: `tests/pages/story.test.tsx`

- [ ] **Step 1: Написать падающий тест на ChapterListItem**

```tsx
// tests/components/story/ChapterListItem.test.tsx
// @vitest-environment jsdom
import { describe, it, expect } from 'vitest';
import { render, screen } from '@testing-library/react';
import { ChapterListItem } from '@/components/story/ChapterListItem';

describe('ChapterListItem', () => {
  it('renders chapter number, title, and minutes', () => {
    render(
      <ChapterListItem
        storyId="s1"
        chapter={{ n: 7, title: 'полночное чтиво', minutes: 4, state: 'reading' }}
      />,
    );
    expect(screen.getByText('07')).toBeInTheDocument();
    expect(screen.getByText(/полночное чтиво/i)).toBeInTheDocument();
    expect(screen.getByText(/4m/i)).toBeInTheDocument();
  });

  it('shows continue marker when state=reading', () => {
    render(
      <ChapterListItem storyId="s1" chapter={{ n: 7, title: 'a', minutes: 4, state: 'reading' }} />,
    );
    expect(screen.getByText(/продолжить/i)).toBeInTheDocument();
  });

  it('shows read marker when state=read', () => {
    render(
      <ChapterListItem storyId="s1" chapter={{ n: 1, title: 'a', minutes: 4, state: 'read' }} />,
    );
    expect(screen.getByText(/прочитано/i)).toBeInTheDocument();
  });

  it('renders a non-clickable link when locked', () => {
    render(
      <ChapterListItem storyId="s1" chapter={{ n: 14, title: 'a', minutes: 8, state: 'locked' }} />,
    );
    const link = screen.queryByRole('link');
    expect(link).toBeNull();
  });
});
```

- [ ] **Step 2: Запустить тест — должен упасть**

Run: `pnpm test tests/components/story/ChapterListItem.test.tsx`
Expected: FAIL.

- [ ] **Step 3: Реализовать ChapterListItem**

```tsx
// components/story/ChapterListItem.tsx
'use client';

import Link from 'next/link';
import { track } from '@/lib/track';
import type { Chapter } from '@/lib/types/story';

const stateLabel: Record<Chapter['state'], string> = {
  read: '✓ прочитано',
  reading: 'продолжить →',
  unread: 'дальше',
  locked: 'заблокировано',
  generating: 'пишется...',
};

const stateColor: Record<Chapter['state'], string> = {
  read: 'text-ink-dim',
  reading: 'text-amber-default',
  unread: 'text-ink-default',
  locked: 'text-ink-faint',
  generating: 'text-amber-default animate-pulse',
};

type Props = { storyId: string; chapter: Chapter };

export function ChapterListItem({ storyId, chapter }: Props) {
  const padded = String(chapter.n).padStart(2, '0');
  const content = (
    <div className="flex items-baseline gap-4 border-b border-ink-faint/10 px-4 py-4">
      <span
        className={`font-display text-3xl ${
          chapter.state === 'reading'
            ? 'text-amber-default'
            : chapter.state === 'read'
              ? 'text-ink-dim'
              : chapter.state === 'locked'
                ? 'text-ink-faint'
                : 'text-ink-default'
        }`}
      >
        {padded}
      </span>
      <div className="flex flex-1 flex-col gap-1">
        <span className="font-display italic">{chapter.title}</span>
        <span
          className={`font-mono text-[10px] uppercase tracking-wider ${stateColor[chapter.state]}`}
        >
          {chapter.minutes}m · {stateLabel[chapter.state]}
        </span>
      </div>
    </div>
  );

  if (chapter.state === 'locked') {
    return content;
  }

  return (
    <Link
      href={`/reader/${storyId}/${chapter.n}`}
      onClick={() => track('story_chapter_tap', { n: chapter.n, state: chapter.state })}
    >
      {content}
    </Link>
  );
}
```

- [ ] **Step 4: Запустить тест**

Run: `pnpm test tests/components/story/ChapterListItem.test.tsx`
Expected: PASS (4 tests).

- [ ] **Step 5: Реализовать StoryHero**

```tsx
// components/story/StoryHero.tsx
import { GrainCover } from '@/components/ui/GrainCover';
import { BurstSticker } from '@/components/ui/BurstSticker';
import type { Story } from '@/lib/types/story';

export function StoryHero({ story }: { story: Story }) {
  return (
    <div className="relative aspect-[4/3]">
      <GrainCover
        color1={story.fandom.color1}
        color2={story.fandom.color2}
        className="h-full w-full"
      >
        <BurstSticker className="absolute right-4 top-4 rotate-[8deg]">★ хит</BurstSticker>
      </GrainCover>
    </div>
  );
}
```

- [ ] **Step 6: Написать падающий тест на StoryMetaPanel**

```tsx
// tests/components/story/StoryMetaPanel.test.tsx
// @vitest-environment jsdom
import { describe, it, expect } from 'vitest';
import { render, screen } from '@testing-library/react';
import { StoryMetaPanel } from '@/components/story/StoryMetaPanel';
import { heroStory } from '@/lib/fixtures/stories';

describe('StoryMetaPanel', () => {
  it('renders author handle, chapter count, and likes', () => {
    render(<StoryMetaPanel story={heroStory} />);
    expect(screen.getByText(/palmetto/i)).toBeInTheDocument();
    expect(screen.getByText(/14/)).toBeInTheDocument();
    expect(screen.getByText(/24\.8k/)).toBeInTheDocument();
  });
});
```

- [ ] **Step 7: Запустить тест — должен упасть**

Run: `pnpm test tests/components/story/StoryMetaPanel.test.tsx`
Expected: FAIL.

- [ ] **Step 8: Реализовать StoryMetaPanel**

```tsx
// components/story/StoryMetaPanel.tsx
import type { Story } from '@/lib/types/story';

export function StoryMetaPanel({ story }: { story: Story }) {
  return (
    <div className="grid grid-cols-3 gap-4 bg-amber-soft px-4 py-4 font-mono text-[10px] uppercase tracking-wider text-bg-deep">
      <div className="flex flex-col gap-1">
        <span className="opacity-60">author</span>
        <span>@{story.author.handle}</span>
      </div>
      <div className="flex flex-col gap-1">
        <span className="opacity-60">chapters</span>
        <span>{story.chapters} / ∞</span>
      </div>
      <div className="flex flex-col gap-1">
        <span className="opacity-60">likes</span>
        <span>♡ {(story.likes / 1000).toFixed(1)}k</span>
      </div>
    </div>
  );
}
```

- [ ] **Step 9: Запустить тест**

Run: `pnpm test tests/components/story/StoryMetaPanel.test.tsx`
Expected: PASS.

- [ ] **Step 10: Реализовать WatchCtaCard**

```tsx
// components/story/WatchCtaCard.tsx
'use client';

import Link from 'next/link';
import { track } from '@/lib/track';
import type { Story } from '@/lib/types/story';

export function WatchCtaCard({ story }: { story: Story }) {
  if (!story.hasWatch) return null;
  return (
    <Link
      href={`/reader/${story.id}/1`}
      onClick={() => track('story_watch_tap')}
      className="mx-4 flex items-center justify-between rounded-lg border border-chrome-default/40 bg-bg-deep p-4"
    >
      <div className="flex flex-col gap-1">
        <span className="font-mono text-[10px] uppercase tracking-wider text-chrome-default">
          ▸ watch mode
        </span>
        <span className="font-display text-sm">слушай как мини-сериал</span>
      </div>
      <span className="text-2xl text-amber-default">●</span>
    </Link>
  );
}
```

- [ ] **Step 11: Прописать страницу `/story/[id]`**

```tsx
// app/(reader)/story/[id]/page.tsx
'use client';

import { use, useEffect } from 'react';
import Link from 'next/link';
import { notFound } from 'next/navigation';
import { StoryHero } from '@/components/story/StoryHero';
import { StoryMetaPanel } from '@/components/story/StoryMetaPanel';
import { WatchCtaCard } from '@/components/story/WatchCtaCard';
import { ChapterListItem } from '@/components/story/ChapterListItem';
import { Ornament } from '@/components/ui/Ornament';
import { MonoBadge } from '@/components/ui/MonoBadge';
import { getStoryDetail } from '@/lib/fixtures/chapters';
import { track } from '@/lib/track';

export default function StoryPage({ params }: { params: Promise<{ id: string }> }) {
  const { id } = use(params);
  const detail = getStoryDetail(id);

  useEffect(() => {
    if (detail) track('story_viewed', { story_id: detail.story.id, source: 'feed' });
  }, [detail]);

  if (!detail) notFound();

  const { story, chapters, progress, watchAvailable } = detail;
  const continueChapter = progress?.lastChapterN ?? 1;
  const ctaLabel = progress ? `★ продолжить гл. ${continueChapter} ★` : '★ начать ★';

  return (
    <div className="min-h-screen bg-bg-deep pb-24 text-ink-default">
      <header className="flex items-center justify-between px-4 py-3">
        <Link href="/" aria-label="back" className="font-mono text-base">
          ←
        </Link>
        <div className="flex gap-3 font-mono text-base">
          <button
            type="button"
            aria-label="save"
            onClick={() => track('story_save_toggle', { saved: !detail.saved })}
          >
            ♡
          </button>
          <button type="button" aria-label="share" onClick={() => track('story_share')}>
            ↗
          </button>
          <button type="button" aria-label="more">
            ⋯
          </button>
        </div>
      </header>

      <StoryHero story={story} />

      <div className="flex flex-col gap-3 px-4 py-5">
        <MonoBadge variant="amber">
          vol.1 · {story.pair} · {story.chapters} ch
        </MonoBadge>
        <h1 className="font-display text-3xl text-balance">{story.title}</h1>
        <p className="font-body text-sm italic text-ink-dim">{story.tagline}</p>
      </div>

      <StoryMetaPanel story={story} />

      {watchAvailable ? (
        <div className="py-4">
          <WatchCtaCard story={story} />
        </div>
      ) : null}

      <Ornament className="py-6">★ главы ★</Ornament>

      <ul className="flex flex-col">
        {chapters.map((ch) => (
          <li key={ch.n}>
            <ChapterListItem storyId={story.id} chapter={ch} />
          </li>
        ))}
      </ul>

      <div className="fixed inset-x-0 bottom-0 border-t border-ink-faint/20 bg-bg-deep/95 p-3 backdrop-blur lg:hidden">
        <Link
          href={`/reader/${story.id}/${continueChapter}`}
          onClick={() => track('story_continue_tap', { last_n: continueChapter })}
          className="block w-full rounded-full bg-amber-default py-3 text-center font-mono text-xs uppercase tracking-wider text-bg-deep"
        >
          {ctaLabel}
        </Link>
      </div>
    </div>
  );
}
```

- [ ] **Step 12: Smoke-тест страницы**

```tsx
// tests/pages/story.test.tsx
// @vitest-environment jsdom
import { describe, it, expect } from 'vitest';
import { render, screen } from '@testing-library/react';
import StoryPage from '@/app/(reader)/story/[id]/page';

describe('Story page', () => {
  it('renders story title, meta panel, and chapter list when id matches', async () => {
    const ui = await StoryPage({ params: Promise.resolve({ id: 'hero-1' }) });
    render(ui);
    expect(screen.getByRole('heading', { level: 1 })).toBeInTheDocument();
    expect(screen.getByText(/palmetto/i)).toBeInTheDocument();
    expect(screen.getByText('07')).toBeInTheDocument(); // chapter 7
  });
});
```

Note: `use(params)` нельзя вызвать в RTL без Suspense. Если падает — заменить компонент на принимающий resolved params, либо обернуть `<Suspense>`. Альтернатива — извлечь чистую функцию `<StoryPageView detail={detail} />` и тестировать её.

- [ ] **Step 13: Если smoke-тест падает на `use(params)`** — рефакторнуть

Извлечь UI-часть:

```tsx
// app/(reader)/story/[id]/page.tsx — обёртка
'use client';
import { use } from 'react';
import { StoryPageView } from './StoryPageView';
export default function StoryPage({ params }: { params: Promise<{ id: string }> }) {
  const { id } = use(params);
  return <StoryPageView id={id} />;
}
```

И `StoryPageView` — это та компонента, что в Step 11, но принимает `id: string` напрямую. Тест импортирует `StoryPageView` и рендерит с `id="hero-1"`.

- [ ] **Step 14: Запустить все тесты + typecheck + lint**

Run: `pnpm test && pnpm typecheck && pnpm lint`
Expected: all PASS.

- [ ] **Step 15: Commit**

```bash
git add app/\(reader\)/ components/story/ tests/components/story/ tests/pages/story.test.tsx
git commit -m "feat(M1-03): story page with meta panel, watch cta, chapter list"
```

---

## Task 6: useReaderSettings hook

**Files:**

- Create: `lib/reader/useReaderSettings.ts`
- Test: `tests/lib/reader/useReaderSettings.test.ts`

**Контекст:** Хук возвращает `{settings, setSetting}`. `settings` персистится в `localStorage` под ключом `headcanon:reader-settings`. Defaults — из spec'а `04-reader.md`. На SSR/первом рендере отдаём defaults; localStorage читаем в effect.

- [ ] **Step 1: Написать падающие тесты**

```ts
// tests/lib/reader/useReaderSettings.test.ts
// @vitest-environment jsdom
import { describe, it, expect, beforeEach } from 'vitest';
import { renderHook, act } from '@testing-library/react';
import { useReaderSettings, DEFAULT_SETTINGS } from '@/lib/reader/useReaderSettings';

describe('useReaderSettings', () => {
  beforeEach(() => {
    localStorage.clear();
  });

  it('starts with defaults when localStorage is empty', () => {
    const { result } = renderHook(() => useReaderSettings());
    expect(result.current.settings).toEqual(DEFAULT_SETTINGS);
  });

  it('persists changes to localStorage', () => {
    const { result } = renderHook(() => useReaderSettings());
    act(() => {
      result.current.setSetting('font', 'bodoni');
    });
    expect(result.current.settings.font).toBe('bodoni');
    const stored = JSON.parse(localStorage.getItem('headcanon:reader-settings')!);
    expect(stored.font).toBe('bodoni');
  });

  it('hydrates from localStorage on mount', () => {
    localStorage.setItem(
      'headcanon:reader-settings',
      JSON.stringify({ ...DEFAULT_SETTINGS, fontSize: 18, theme: 'sepia' }),
    );
    const { result } = renderHook(() => useReaderSettings());
    expect(result.current.settings.fontSize).toBe(18);
    expect(result.current.settings.theme).toBe('sepia');
  });

  it('ignores corrupt JSON', () => {
    localStorage.setItem('headcanon:reader-settings', '{not json');
    const { result } = renderHook(() => useReaderSettings());
    expect(result.current.settings).toEqual(DEFAULT_SETTINGS);
  });
});
```

- [ ] **Step 2: Запустить тест — должен упасть**

Run: `pnpm test tests/lib/reader/useReaderSettings.test.ts`
Expected: FAIL — module not found.

- [ ] **Step 3: Реализовать хук**

```ts
// lib/reader/useReaderSettings.ts
'use client';

import { useEffect, useState, useCallback } from 'react';

export type ReaderFont = 'bodoni' | 'garamond' | 'cormorant';
export type ReaderTheme = 'late-night' | 'sepia' | 'true-dark';

export type ReaderSettings = {
  font: ReaderFont;
  fontSize: number;
  theme: ReaderTheme;
  justify: boolean;
  hyphens: boolean;
};

export const DEFAULT_SETTINGS: ReaderSettings = {
  font: 'garamond',
  fontSize: 16,
  theme: 'late-night',
  justify: true,
  hyphens: true,
};

const STORAGE_KEY = 'headcanon:reader-settings';

export function useReaderSettings() {
  const [settings, setSettings] = useState<ReaderSettings>(DEFAULT_SETTINGS);

  useEffect(() => {
    try {
      const raw = localStorage.getItem(STORAGE_KEY);
      if (!raw) return;
      const parsed = JSON.parse(raw) as Partial<ReaderSettings>;
      setSettings((prev) => ({ ...prev, ...parsed }));
    } catch {
      // ignore corrupt JSON
    }
  }, []);

  const setSetting = useCallback(
    <K extends keyof ReaderSettings>(key: K, value: ReaderSettings[K]) => {
      setSettings((prev) => {
        const next = { ...prev, [key]: value };
        try {
          localStorage.setItem(STORAGE_KEY, JSON.stringify(next));
        } catch {
          // ignore quota errors
        }
        return next;
      });
    },
    [],
  );

  return { settings, setSetting };
}
```

- [ ] **Step 4: Запустить тест**

Run: `pnpm test tests/lib/reader/useReaderSettings.test.ts`
Expected: PASS (4 tests).

- [ ] **Step 5: Commit**

```bash
git add lib/reader/useReaderSettings.ts tests/lib/reader/
git commit -m "feat(M1-04): useReaderSettings hook (localStorage + defaults)"
```

---

## Task 7: Reader screen (M1-04)

**Files:**

- Create: `app/(reader)/reader/[storyId]/[chapterN]/page.tsx`
- Create: `components/reader/ReaderTopBar.tsx`
- Create: `components/reader/ReaderProgressBar.tsx`
- Create: `components/reader/ReaderBody.tsx`
- Create: `components/reader/ReaderSettingsSheet.tsx`
- Create: `components/reader/NextChapterCard.tsx`
- Test: `tests/components/reader/ReaderProgressBar.test.tsx`
- Test: `tests/components/reader/ReaderSettingsSheet.test.tsx`
- Test: `tests/pages/reader.test.tsx`

- [ ] **Step 1: Написать падающий тест на ReaderProgressBar**

```tsx
// tests/components/reader/ReaderProgressBar.test.tsx
// @vitest-environment jsdom
import { describe, it, expect } from 'vitest';
import { render, screen } from '@testing-library/react';
import { ReaderProgressBar } from '@/components/reader/ReaderProgressBar';

describe('ReaderProgressBar', () => {
  it('renders progress and page label', () => {
    render(<ReaderProgressBar percent={33} pageLabel="стр 3 / 9" />);
    const bar = screen.getByRole('progressbar');
    expect(bar).toHaveAttribute('aria-valuenow', '33');
    expect(screen.getByText('стр 3 / 9')).toBeInTheDocument();
  });

  it('clamps percent to [0, 100]', () => {
    render(<ReaderProgressBar percent={150} pageLabel="" />);
    expect(screen.getByRole('progressbar')).toHaveAttribute('aria-valuenow', '100');
  });
});
```

- [ ] **Step 2: Запустить тест**

Run: `pnpm test tests/components/reader/ReaderProgressBar.test.tsx`
Expected: FAIL.

- [ ] **Step 3: Реализовать ReaderProgressBar**

```tsx
// components/reader/ReaderProgressBar.tsx
type Props = { percent: number; pageLabel: string };

export function ReaderProgressBar({ percent, pageLabel }: Props) {
  const clamped = Math.max(0, Math.min(100, percent));
  return (
    <div className="sticky top-12 z-20 flex items-center gap-3 bg-bg-deep px-4 py-1">
      <div
        role="progressbar"
        aria-valuenow={clamped}
        aria-valuemin={0}
        aria-valuemax={100}
        className="h-0.5 flex-1 bg-ink-faint/20"
      >
        <div
          className="h-full bg-amber-default transition-[width]"
          style={{ width: `${clamped}%` }}
        />
      </div>
      <span className="font-mono text-[9px] uppercase tracking-wider text-ink-dim">
        {pageLabel}
      </span>
    </div>
  );
}
```

- [ ] **Step 4: Запустить тест**

Run: `pnpm test tests/components/reader/ReaderProgressBar.test.tsx`
Expected: PASS (2 tests).

- [ ] **Step 5: Реализовать ReaderTopBar**

```tsx
// components/reader/ReaderTopBar.tsx
import Link from 'next/link';

type Props = {
  storyId: string;
  chapterN: number;
  chapterTitle: string;
  onOpenSettings: () => void;
};

export function ReaderTopBar({ storyId, chapterN, chapterTitle, onOpenSettings }: Props) {
  return (
    <header className="sticky top-0 z-20 flex items-center justify-between gap-3 border-b border-ink-faint/10 bg-bg-deep/95 px-4 py-2 backdrop-blur">
      <Link href={`/story/${storyId}`} aria-label="back" className="font-mono text-base">
        ←
      </Link>
      <div className="flex flex-col items-center text-center">
        <span className="font-mono text-[10px] uppercase tracking-wider text-ink-dim">
          vol.1 · ch.{String(chapterN).padStart(2, '0')}
        </span>
        <span className="font-display text-xs italic text-amber-default">{chapterTitle}</span>
      </div>
      <div className="flex gap-3">
        <button
          type="button"
          onClick={onOpenSettings}
          aria-label="settings"
          className="font-display text-base"
        >
          Aa
        </button>
        <button type="button" aria-label="more" className="font-mono text-base">
          ⚙
        </button>
      </div>
    </header>
  );
}
```

- [ ] **Step 6: Реализовать ReaderBody с drop cap**

```tsx
// components/reader/ReaderBody.tsx
import { Ornament } from '@/components/ui/Ornament';
import type { ReaderSettings } from '@/lib/reader/useReaderSettings';

type Props = {
  paragraphs: string[];
  settings: ReaderSettings;
};

export function ReaderBody({ paragraphs, settings }: Props) {
  const fontFamilyClass =
    settings.font === 'bodoni'
      ? 'font-display'
      : settings.font === 'cormorant'
        ? 'font-display' // M1: cormorant пока не подключён, используем display fallback
        : 'font-body';

  return (
    <article
      className={`mx-auto max-w-[660px] px-4 py-6 ${fontFamilyClass}`}
      style={{
        fontSize: `${settings.fontSize}px`,
        lineHeight: 1.6,
        textAlign: settings.justify ? 'justify' : 'left',
        hyphens: settings.hyphens ? 'auto' : 'manual',
      }}
    >
      {paragraphs.map((p, i) => (
        <p
          key={i}
          className={i === 0 ? 'reader-first-paragraph' : ''}
          style={{ marginBottom: '1em' }}
        >
          {i === 0 ? (
            <>
              <span
                style={{
                  float: 'left',
                  fontFamily: 'var(--font-display)',
                  fontStyle: 'italic',
                  fontSize: '4em',
                  lineHeight: 0.85,
                  color: 'var(--color-amber-default, #d97706)',
                  paddingRight: '0.1em',
                  marginTop: '0.05em',
                }}
              >
                {p.charAt(0)}
              </span>
              {p.slice(1)}
            </>
          ) : (
            p
          )}
          {(i + 1) % 4 === 0 && i !== paragraphs.length - 1 ? (
            <Ornament className="my-6">─ ✦ ─</Ornament>
          ) : null}
        </p>
      ))}
    </article>
  );
}
```

- [ ] **Step 7: Написать падающий тест на ReaderSettingsSheet**

```tsx
// tests/components/reader/ReaderSettingsSheet.test.tsx
// @vitest-environment jsdom
import { describe, it, expect, vi } from 'vitest';
import { render, screen, fireEvent } from '@testing-library/react';
import { ReaderSettingsSheet } from '@/components/reader/ReaderSettingsSheet';
import { DEFAULT_SETTINGS } from '@/lib/reader/useReaderSettings';

describe('ReaderSettingsSheet', () => {
  it('renders font, size, theme controls when open', () => {
    render(
      <ReaderSettingsSheet
        open
        settings={DEFAULT_SETTINGS}
        onChange={() => {}}
        onClose={() => {}}
      />,
    );
    expect(screen.getByText(/шрифт/i)).toBeInTheDocument();
    expect(screen.getByText(/размер/i)).toBeInTheDocument();
    expect(screen.getByText(/тема/i)).toBeInTheDocument();
  });

  it('calls onChange when font button clicked', () => {
    const onChange = vi.fn();
    render(
      <ReaderSettingsSheet
        open
        settings={DEFAULT_SETTINGS}
        onChange={onChange}
        onClose={() => {}}
      />,
    );
    fireEvent.click(screen.getByRole('button', { name: /bodoni/i }));
    expect(onChange).toHaveBeenCalledWith('font', 'bodoni');
  });

  it('renders nothing when closed', () => {
    const { container } = render(
      <ReaderSettingsSheet
        open={false}
        settings={DEFAULT_SETTINGS}
        onChange={() => {}}
        onClose={() => {}}
      />,
    );
    expect(container).toBeEmptyDOMElement();
  });
});
```

- [ ] **Step 8: Запустить тест**

Run: `pnpm test tests/components/reader/ReaderSettingsSheet.test.tsx`
Expected: FAIL.

- [ ] **Step 9: Реализовать ReaderSettingsSheet**

```tsx
// components/reader/ReaderSettingsSheet.tsx
'use client';

import type { ReaderSettings, ReaderFont, ReaderTheme } from '@/lib/reader/useReaderSettings';

type Props = {
  open: boolean;
  settings: ReaderSettings;
  onChange: <K extends keyof ReaderSettings>(key: K, value: ReaderSettings[K]) => void;
  onClose: () => void;
};

const fonts: { value: ReaderFont; label: string }[] = [
  { value: 'bodoni', label: 'Bodoni Moda' },
  { value: 'garamond', label: 'EB Garamond' },
  { value: 'cormorant', label: 'Cormorant' },
];

const themes: { value: ReaderTheme; label: string }[] = [
  { value: 'late-night', label: 'late night' },
  { value: 'sepia', label: 'sepia' },
  { value: 'true-dark', label: 'true dark' },
];

export function ReaderSettingsSheet({ open, settings, onChange, onClose }: Props) {
  if (!open) return null;
  return (
    <>
      <button
        type="button"
        aria-label="close settings"
        onClick={onClose}
        className="fixed inset-0 z-30 bg-bg-deep/60 backdrop-blur-sm"
      />
      <section
        role="dialog"
        aria-label="настройки чтения"
        className="fixed inset-x-0 bottom-0 z-40 flex flex-col gap-5 rounded-t-2xl bg-bg-deep p-6 lg:max-w-md lg:left-1/2 lg:-translate-x-1/2"
      >
        <div className="flex flex-col gap-2">
          <span className="font-mono text-[10px] uppercase tracking-wider text-ink-dim">шрифт</span>
          <div className="flex gap-2">
            {fonts.map((f) => (
              <button
                key={f.value}
                type="button"
                onClick={() => onChange('font', f.value)}
                className={`rounded-full border px-3 py-1.5 font-mono text-xs uppercase ${
                  settings.font === f.value
                    ? 'border-amber-default bg-amber-default text-bg-deep'
                    : 'border-ink-faint/30 text-ink-default'
                }`}
              >
                {f.label}
              </button>
            ))}
          </div>
        </div>

        <div className="flex flex-col gap-2">
          <span className="font-mono text-[10px] uppercase tracking-wider text-ink-dim">
            размер: {settings.fontSize}px
          </span>
          <div className="flex items-center gap-3">
            <button
              type="button"
              onClick={() => onChange('fontSize', Math.max(14, settings.fontSize - 1))}
              className="rounded-full border border-ink-faint/30 px-3 py-1 font-mono"
              aria-label="decrease size"
            >
              −
            </button>
            <input
              type="range"
              min={14}
              max={22}
              value={settings.fontSize}
              onChange={(e) => onChange('fontSize', Number(e.target.value))}
              className="flex-1"
            />
            <button
              type="button"
              onClick={() => onChange('fontSize', Math.min(22, settings.fontSize + 1))}
              className="rounded-full border border-ink-faint/30 px-3 py-1 font-mono"
              aria-label="increase size"
            >
              +
            </button>
          </div>
        </div>

        <div className="flex flex-col gap-2">
          <span className="font-mono text-[10px] uppercase tracking-wider text-ink-dim">тема</span>
          <div className="flex gap-2">
            {themes.map((t) => (
              <button
                key={t.value}
                type="button"
                onClick={() => onChange('theme', t.value)}
                className={`rounded-full border px-3 py-1.5 font-mono text-xs uppercase ${
                  settings.theme === t.value
                    ? 'border-amber-default bg-amber-default text-bg-deep'
                    : 'border-ink-faint/30 text-ink-default'
                }`}
              >
                {t.label}
              </button>
            ))}
          </div>
        </div>

        <div className="flex justify-between font-mono text-xs uppercase">
          <label className="flex items-center gap-2">
            <input
              type="checkbox"
              checked={settings.justify}
              onChange={(e) => onChange('justify', e.target.checked)}
            />
            justify
          </label>
          <label className="flex items-center gap-2">
            <input
              type="checkbox"
              checked={settings.hyphens}
              onChange={(e) => onChange('hyphens', e.target.checked)}
            />
            hyphens
          </label>
        </div>
      </section>
    </>
  );
}
```

- [ ] **Step 10: Запустить тест**

Run: `pnpm test tests/components/reader/ReaderSettingsSheet.test.tsx`
Expected: PASS (3 tests).

- [ ] **Step 11: Реализовать NextChapterCard**

```tsx
// components/reader/NextChapterCard.tsx
'use client';

import Link from 'next/link';
import { track } from '@/lib/track';
import type { Chapter } from '@/lib/types/story';

type Props = { storyId: string; nextChapter: Chapter | null; hasWatch: boolean };

export function NextChapterCard({ storyId, nextChapter, hasWatch }: Props) {
  if (!nextChapter) return null;
  return (
    <div className="mx-auto mt-12 max-w-[660px] border-t border-ink-faint/20 px-4 py-6">
      <div className="flex items-center justify-between gap-4">
        <Link href={`/reader/${storyId}/${nextChapter.n}`} className="flex flex-col gap-1">
          <span className="font-mono text-[10px] uppercase tracking-wider text-ink-dim">
            следующая глава
          </span>
          <span className="font-display italic">{nextChapter.title}</span>
          <span className="font-mono text-[10px] text-ink-dim">{nextChapter.minutes}m</span>
        </Link>
        {hasWatch ? (
          <Link
            href={`/watch/${storyId}/${nextChapter.n}`}
            onClick={() => track('reader_watch_chip_tap')}
            className="rounded-full border border-chrome-default px-3 py-1.5 font-mono text-[10px] uppercase tracking-wider text-chrome-default"
          >
            ▸ watch
          </Link>
        ) : null}
      </div>
    </div>
  );
}
```

- [ ] **Step 12: Прописать страницу `/reader/[storyId]/[chapterN]`**

```tsx
// app/(reader)/reader/[storyId]/[chapterN]/page.tsx
'use client';

import { use, useEffect, useState } from 'react';
import { notFound } from 'next/navigation';
import { ReaderTopBar } from '@/components/reader/ReaderTopBar';
import { ReaderProgressBar } from '@/components/reader/ReaderProgressBar';
import { ReaderBody } from '@/components/reader/ReaderBody';
import { ReaderSettingsSheet } from '@/components/reader/ReaderSettingsSheet';
import { NextChapterCard } from '@/components/reader/NextChapterCard';
import { Ornament } from '@/components/ui/Ornament';
import { useReaderSettings } from '@/lib/reader/useReaderSettings';
import { getStoryDetail, getChapterContent } from '@/lib/fixtures/chapters';
import { track } from '@/lib/track';

type Params = { storyId: string; chapterN: string };

export default function ReaderPage({ params }: { params: Promise<Params> }) {
  const { storyId, chapterN } = use(params);
  const n = Number(chapterN);
  const detail = getStoryDetail(storyId);
  const content = getChapterContent(storyId, n);
  const { settings, setSetting } = useReaderSettings();
  const [settingsOpen, setSettingsOpen] = useState(false);

  useEffect(() => {
    if (detail && content) {
      track('reader_opened', {
        story_id: storyId,
        chapter_n: n,
        has_progress: detail.progress !== null,
      });
    }
  }, [storyId, n, detail, content]);

  if (!detail || !content) notFound();

  const nextChapter = detail.chapters.find((c) => c.n === n + 1) ?? null;

  return (
    <div className="min-h-screen bg-bg-deep text-ink-default">
      <ReaderTopBar
        storyId={storyId}
        chapterN={n}
        chapterTitle={content.title}
        onOpenSettings={() => setSettingsOpen(true)}
      />
      <ReaderProgressBar percent={0} pageLabel="стр 1 / 9" />
      <div className="mx-auto max-w-[660px] px-4 pt-6">
        <span className="font-display text-sm italic text-amber-default">
          глава {n === 7 ? 'седьмая' : n}
        </span>
        <h1 className="mt-2 font-display text-3xl text-balance">{content.title}</h1>
        <Ornament className="my-4">─ ✦ ─</Ornament>
      </div>
      <ReaderBody paragraphs={content.paragraphs} settings={settings} />
      <NextChapterCard
        storyId={storyId}
        nextChapter={nextChapter}
        hasWatch={detail.watchAvailable}
      />
      <ReaderSettingsSheet
        open={settingsOpen}
        settings={settings}
        onChange={(k, v) => {
          setSetting(k, v);
          track('reader_settings_changed', { key: k, value: v });
        }}
        onClose={() => setSettingsOpen(false)}
      />
    </div>
  );
}
```

- [ ] **Step 13: Smoke-тест страницы reader**

```tsx
// tests/pages/reader.test.tsx
// @vitest-environment jsdom
import { describe, it, expect } from 'vitest';
import { render, screen } from '@testing-library/react';
import ReaderPage from '@/app/(reader)/reader/[storyId]/[chapterN]/page';

describe('Reader page', () => {
  it('renders chapter title and first paragraph for hero ch.7', async () => {
    const ui = await ReaderPage({
      params: Promise.resolve({ storyId: 'hero-1', chapterN: '7' }),
    });
    render(ui);
    expect(screen.getByRole('heading', { level: 1 })).toHaveTextContent(/полночное чтиво/i);
    expect(screen.getByText(/Крыша всё ещё держала/i)).toBeInTheDocument();
  });
});
```

Если падает на `use(params)` — извлечь `<ReaderPageView storyId chapterN />` так же, как в Task 5 Step 13.

- [ ] **Step 14: Запустить все тесты + typecheck + lint**

Run: `pnpm test && pnpm typecheck && pnpm lint`
Expected: all PASS.

- [ ] **Step 15: Commit**

```bash
git add app/\(reader\)/reader/ components/reader/ tests/components/reader/ tests/pages/reader.test.tsx
git commit -m "feat(M1-04): reader screen with drop cap, settings sheet, next chapter card"
```

---

## Task 8: useReadingProgress hook (IntersectionObserver)

**Files:**

- Create: `lib/reader/useReadingProgress.ts`
- Test: `tests/lib/reader/useReadingProgress.test.ts`
- Modify: `app/(reader)/reader/[storyId]/[chapterN]/page.tsx` — подключить хук, обновлять `<ReaderProgressBar>` и слать `reader_progress_milestone`

**Контекст:** хук считает progress (0-100) по доле параграфов, которые попали в viewport. Также шлёт `reader_progress_milestone` на 25/50/75/100. IntersectionObserver — `threshold: 0.5`.

- [ ] **Step 1: Написать падающий тест**

```ts
// tests/lib/reader/useReadingProgress.test.ts
// @vitest-environment jsdom
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { renderHook, act } from '@testing-library/react';
import { useReadingProgress } from '@/lib/reader/useReadingProgress';

class MockObserver {
  callback: IntersectionObserverCallback;
  static instances: MockObserver[] = [];
  observe = vi.fn();
  disconnect = vi.fn();
  unobserve = vi.fn();
  takeRecords = vi.fn(() => []);
  root = null;
  rootMargin = '';
  thresholds = [];
  constructor(cb: IntersectionObserverCallback) {
    this.callback = cb;
    MockObserver.instances.push(this);
  }
}

describe('useReadingProgress', () => {
  beforeEach(() => {
    MockObserver.instances = [];
    // @ts-expect-error: stub
    globalThis.IntersectionObserver = MockObserver;
  });

  it('starts at 0 percent', () => {
    const { result } = renderHook(() => useReadingProgress(10));
    expect(result.current.percent).toBe(0);
    expect(result.current.containerRef).toBeDefined();
  });

  it('updates percent when 5 of 10 paragraphs are seen', () => {
    const { result } = renderHook(() => useReadingProgress(10));
    const div = document.createElement('div');
    for (let i = 0; i < 10; i++) {
      const p = document.createElement('p');
      div.appendChild(p);
    }
    act(() => {
      result.current.containerRef.current = div;
      result.current._setupForTest?.();
    });
    const obs = MockObserver.instances[0];
    expect(obs).toBeDefined();
    const entries = Array.from(div.children)
      .slice(0, 5)
      .map((el) => ({ target: el, isIntersecting: true }) as unknown as IntersectionObserverEntry);
    act(() => {
      obs.callback(entries, obs as unknown as IntersectionObserver);
    });
    expect(result.current.percent).toBe(50);
  });
});
```

- [ ] **Step 2: Запустить тест**

Run: `pnpm test tests/lib/reader/useReadingProgress.test.ts`
Expected: FAIL.

- [ ] **Step 3: Реализовать хук**

```ts
// lib/reader/useReadingProgress.ts
'use client';

import { useEffect, useRef, useState, useCallback } from 'react';
import { track } from '@/lib/track';

export function useReadingProgress(totalParagraphs: number) {
  const containerRef = useRef<HTMLElement | null>(null);
  const [percent, setPercent] = useState(0);
  const seenRef = useRef<Set<Element>>(new Set());
  const milestonesFiredRef = useRef<Set<number>>(new Set());

  const setup = useCallback(() => {
    const el = containerRef.current;
    if (!el || typeof IntersectionObserver === 'undefined') return;
    const paragraphs = Array.from(el.children) as Element[];
    const observer = new IntersectionObserver(
      (entries) => {
        for (const e of entries) {
          if (e.isIntersecting) seenRef.current.add(e.target);
        }
        const next =
          totalParagraphs === 0
            ? 0
            : Math.min(100, Math.round((seenRef.current.size / totalParagraphs) * 100));
        setPercent(next);
        for (const ms of [25, 50, 75, 100]) {
          if (next >= ms && !milestonesFiredRef.current.has(ms)) {
            milestonesFiredRef.current.add(ms);
            track('reader_progress_milestone', { percent: ms });
          }
        }
      },
      { threshold: 0.5 },
    );
    paragraphs.forEach((p) => observer.observe(p));
    return () => observer.disconnect();
  }, [totalParagraphs]);

  useEffect(() => {
    return setup();
  }, [setup]);

  return {
    percent,
    containerRef,
    _setupForTest: setup,
  };
}
```

- [ ] **Step 4: Запустить тест**

Run: `pnpm test tests/lib/reader/useReadingProgress.test.ts`
Expected: PASS (2 tests).

- [ ] **Step 5: Подключить хук в Reader page**

В `app/(reader)/reader/[storyId]/[chapterN]/page.tsx` импортировать `useReadingProgress`, привязать `containerRef` к article-обёртке `ReaderBody`. Пробросить `percent` в `<ReaderProgressBar percent={percent} ... />`.

Заменить:

```tsx
<ReaderProgressBar percent={0} pageLabel="стр 1 / 9" />
...
<ReaderBody paragraphs={content.paragraphs} settings={settings} />
```

на:

```tsx
const { percent, containerRef } = useReadingProgress(content.paragraphs.length);
const pagesTotal = Math.max(1, Math.ceil(content.paragraphs.length / 2));
const currentPage = Math.max(1, Math.ceil((percent / 100) * pagesTotal) || 1);
...
<ReaderProgressBar percent={percent} pageLabel={`стр ${currentPage} / ${pagesTotal}`} />
...
<ReaderBody paragraphs={content.paragraphs} settings={settings} bodyRef={containerRef} />
```

И добавить `bodyRef?: React.RefObject<HTMLElement | null>` в props `ReaderBody`, навесить на `<article ref={bodyRef as any}>`.

- [ ] **Step 6: Запустить все тесты + typecheck + lint**

Run: `pnpm test && pnpm typecheck && pnpm lint`
Expected: all PASS.

- [ ] **Step 7: Commit**

```bash
git add lib/reader/useReadingProgress.ts tests/lib/reader/useReadingProgress.test.ts components/reader/ReaderBody.tsx app/\(reader\)/reader/
git commit -m "feat(M1-04): reading progress via IntersectionObserver + milestone events"
```

---

## Task 9: useLateNightDim hook

**Files:**

- Create: `lib/reader/useLateNightDim.ts`
- Test: `tests/lib/reader/useLateNightDim.test.ts`
- Modify: `app/(reader)/reader/[storyId]/[chapterN]/page.tsx`

**Контекст:** хук возвращает `dimmed: boolean`, true когда юзер не скроллил >30 минут. Сбрасывает таймер при `scroll` / `pointermove` / `keydown` событиях. Использовать `setTimeout` (не `setInterval`).

- [ ] **Step 1: Написать падающий тест**

```ts
// tests/lib/reader/useLateNightDim.test.ts
// @vitest-environment jsdom
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
import { renderHook, act } from '@testing-library/react';
import { useLateNightDim } from '@/lib/reader/useLateNightDim';

describe('useLateNightDim', () => {
  beforeEach(() => {
    vi.useFakeTimers();
  });
  afterEach(() => {
    vi.useRealTimers();
  });

  it('starts un-dimmed', () => {
    const { result } = renderHook(() => useLateNightDim(1000));
    expect(result.current).toBe(false);
  });

  it('dims after timeout passes without activity', () => {
    const { result } = renderHook(() => useLateNightDim(1000));
    act(() => {
      vi.advanceTimersByTime(1001);
    });
    expect(result.current).toBe(true);
  });

  it('resets when scroll happens', () => {
    const { result } = renderHook(() => useLateNightDim(1000));
    act(() => {
      vi.advanceTimersByTime(900);
      window.dispatchEvent(new Event('scroll'));
      vi.advanceTimersByTime(900);
    });
    expect(result.current).toBe(false);
    act(() => {
      vi.advanceTimersByTime(200);
    });
    expect(result.current).toBe(true);
  });
});
```

- [ ] **Step 2: Запустить тест**

Run: `pnpm test tests/lib/reader/useLateNightDim.test.ts`
Expected: FAIL.

- [ ] **Step 3: Реализовать хук**

```ts
// lib/reader/useLateNightDim.ts
'use client';

import { useEffect, useState, useRef } from 'react';

const DEFAULT_TIMEOUT_MS = 30 * 60 * 1000;

export function useLateNightDim(timeoutMs: number = DEFAULT_TIMEOUT_MS) {
  const [dimmed, setDimmed] = useState(false);
  const timerRef = useRef<ReturnType<typeof setTimeout> | null>(null);

  useEffect(() => {
    const reset = () => {
      setDimmed(false);
      if (timerRef.current) clearTimeout(timerRef.current);
      timerRef.current = setTimeout(() => setDimmed(true), timeoutMs);
    };

    reset();

    const events = ['scroll', 'pointermove', 'keydown'] as const;
    for (const e of events) window.addEventListener(e, reset, { passive: true });
    return () => {
      for (const e of events) window.removeEventListener(e, reset);
      if (timerRef.current) clearTimeout(timerRef.current);
    };
  }, [timeoutMs]);

  return dimmed;
}
```

- [ ] **Step 4: Запустить тест**

Run: `pnpm test tests/lib/reader/useLateNightDim.test.ts`
Expected: PASS (3 tests).

- [ ] **Step 5: Подключить хук в Reader page**

В `app/(reader)/reader/[storyId]/[chapterN]/page.tsx`:

```tsx
import { useLateNightDim } from '@/lib/reader/useLateNightDim';
...
const dimmed = useLateNightDim();
return (
  <div
    className={`min-h-screen text-ink-default transition-colors duration-[2000ms] ${
      dimmed ? 'bg-bg-deepest' : 'bg-bg-deep'
    }`}
  >
    ...
  </div>
);
```

Если `bg-bg-deepest` ещё нет в токенах — использовать `bg-black` или добавить токен. Проверить `handoff/tokens.css`.

- [ ] **Step 6: Запустить все тесты**

Run: `pnpm test && pnpm typecheck && pnpm lint`
Expected: all PASS.

- [ ] **Step 7: Commit**

```bash
git add lib/reader/useLateNightDim.ts tests/lib/reader/useLateNightDim.test.ts app/\(reader\)/reader/
git commit -m "feat(M1-04): late-night dim — fade to deeper bg after 30 min idle"
```

---

## Task 10: Tracking integration audit (M1-05)

**Files:**

- Modify (audit): все экраны и компоненты, чтобы события из spec'ов отправлялись.
- Test: `tests/lib/track-coverage.test.ts` — статический контракт-тест на event names.

**Контекст:** все ивенты из spec'ов уже точечно вставлены в Tasks 3-9. Здесь — пройти по spec'у каждого экрана и убедиться, что все события вызываются хотя бы в одном `onClick`/`useEffect`. Это finalization-таска.

- [ ] **Step 1: Написать contract-тест на event names**

```ts
// tests/lib/track-coverage.test.ts
// @vitest-environment node
import { describe, it, expect } from 'vitest';
import { readFileSync } from 'node:fs';
import { resolve } from 'node:path';
import { glob } from 'fs/promises';

describe('tracking events coverage (spec → code)', () => {
  it('every spec event name appears at least once in source', async () => {
    const required = [
      'feed_viewed',
      'feed_chip_tap',
      'feed_card_tap',
      'story_viewed',
      'story_save_toggle',
      'story_share',
      'story_chapter_tap',
      'story_continue_tap',
      'story_watch_tap',
      'reader_opened',
      'reader_progress_milestone',
      'reader_settings_changed',
      'reader_watch_chip_tap',
    ];
    const root = resolve(__dirname, '../../');
    const files: string[] = [];
    for await (const f of glob('{app,components,lib}/**/*.{ts,tsx}', { cwd: root })) {
      if (f.includes('node_modules') || f.endsWith('.d.ts')) continue;
      files.push(resolve(root, f));
    }
    const haystack = files.map((p) => readFileSync(p, 'utf-8')).join('\n');
    const missing = required.filter(
      (e) => !haystack.includes(`'${e}'`) && !haystack.includes(`"${e}"`),
    );
    expect(missing).toEqual([]);
  });
});
```

Note: `glob` из `fs/promises` нужен Node 22+. В Node 20 заменить на `fast-glob` или ручной обход. Проверить `node --version` — если 20.x, использовать `await import('node:fs/promises').then((fs) => fs.readdir(...))` рекурсивно. Простейший fallback — заинлайнить список файлов руками.

- [ ] **Step 2: Запустить тест — должен пройти, если в Tasks 3-9 всё на месте**

Run: `pnpm test tests/lib/track-coverage.test.ts`
Expected: PASS. Если падает с пропусками — вставить недостающие `track(...)` вызовы в соответствующих компонентах:

- `feed_chip_tap` — `FandomChips.tsx` (✓ Task 4)
- `feed_card_tap` — `FeedGrid.tsx` (✓ Task 3)
- `feed_viewed` — `app/page.tsx` useEffect (✓ Task 4)
- `story_*` — Task 5
- `reader_opened`, `reader_progress_milestone`, `reader_settings_changed`, `reader_watch_chip_tap` — Tasks 7-8

- [ ] **Step 3: Commit**

```bash
git add tests/lib/track-coverage.test.ts
git commit -m "test(M1-05): contract test ensures all spec'd track events fire"
```

---

## Task 11: Storybook stories для M1 компонентов (часть M0-08, расширение)

**Files:**

- Create: `stories/feed/FandomChips.stories.tsx`
- Create: `stories/feed/FeedHeroDesktop.stories.tsx`
- Create: `stories/feed/TabBarMobile.stories.tsx`
- Create: `stories/story/ChapterListItem.stories.tsx`
- Create: `stories/story/WatchCtaCard.stories.tsx`
- Create: `stories/reader/ReaderSettingsSheet.stories.tsx`

**Контекст:** Storybook 10 уже настроен в M0. Добавляем стори для каждого нетривиального компонента — те, у которых есть состояния (active/inactive, open/closed, locked/unlocked).

- [ ] **Step 1: FandomChips story**

```tsx
// stories/feed/FandomChips.stories.tsx
import type { Meta, StoryObj } from '@storybook/nextjs-vite';
import { FandomChips } from '@/components/feed/FandomChips';
import { fandomChips } from '@/lib/fixtures/stories';

const meta: Meta<typeof FandomChips> = {
  title: 'Feed/FandomChips',
  component: FandomChips,
};
export default meta;

type Story = StoryObj<typeof FandomChips>;

export const Default: Story = {
  args: { chips: fandomChips, onSelect: () => {} },
};

export const NoActive: Story = {
  args: { chips: fandomChips.map((c) => ({ ...c, active: false })), onSelect: () => {} },
};
```

- [ ] **Step 2: TabBarMobile story**

```tsx
// stories/feed/TabBarMobile.stories.tsx
import type { Meta, StoryObj } from '@storybook/nextjs-vite';
import { TabBarMobile } from '@/components/feed/TabBarMobile';

const meta: Meta<typeof TabBarMobile> = {
  title: 'Feed/TabBarMobile',
  component: TabBarMobile,
};
export default meta;

type Story = StoryObj<typeof TabBarMobile>;
export const Feed: Story = { args: { active: 'feed' } };
export const Saved: Story = { args: { active: 'saved' } };
```

- [ ] **Step 3: FeedHeroDesktop story**

```tsx
// stories/feed/FeedHeroDesktop.stories.tsx
import type { Meta, StoryObj } from '@storybook/nextjs-vite';
import { FeedHeroDesktop } from '@/components/feed/FeedHeroDesktop';
import { heroStory } from '@/lib/fixtures/stories';

const meta: Meta<typeof FeedHeroDesktop> = {
  title: 'Feed/FeedHeroDesktop',
  component: FeedHeroDesktop,
  parameters: { viewport: { defaultViewport: 'desktop' } },
};
export default meta;

type Story = StoryObj<typeof FeedHeroDesktop>;
export const Default: Story = { args: { story: heroStory } };
export const NoWatch: Story = {
  args: { story: { ...heroStory, hasWatch: false, watchEpisodes: undefined } },
};
```

- [ ] **Step 4: ChapterListItem story**

```tsx
// stories/story/ChapterListItem.stories.tsx
import type { Meta, StoryObj } from '@storybook/nextjs-vite';
import { ChapterListItem } from '@/components/story/ChapterListItem';

const meta: Meta<typeof ChapterListItem> = {
  title: 'Story/ChapterListItem',
  component: ChapterListItem,
};
export default meta;

type Story = StoryObj<typeof ChapterListItem>;

export const Read: Story = {
  args: { storyId: 's1', chapter: { n: 1, title: 'первый матч', minutes: 4, state: 'read' } },
};
export const Reading: Story = {
  args: {
    storyId: 's1',
    chapter: { n: 7, title: 'полночное чтиво', minutes: 4, state: 'reading' },
  },
};
export const Unread: Story = {
  args: {
    storyId: 's1',
    chapter: { n: 8, title: 'утро воскресенья', minutes: 5, state: 'unread' },
  },
};
export const Locked: Story = {
  args: { storyId: 's1', chapter: { n: 14, title: 'финал сезона', minutes: 8, state: 'locked' } },
};
export const Generating: Story = {
  args: { storyId: 's1', chapter: { n: 9, title: 'пишется', minutes: 5, state: 'generating' } },
};
```

- [ ] **Step 5: WatchCtaCard story**

```tsx
// stories/story/WatchCtaCard.stories.tsx
import type { Meta, StoryObj } from '@storybook/nextjs-vite';
import { WatchCtaCard } from '@/components/story/WatchCtaCard';
import { heroStory } from '@/lib/fixtures/stories';

const meta: Meta<typeof WatchCtaCard> = {
  title: 'Story/WatchCtaCard',
  component: WatchCtaCard,
};
export default meta;

type Story = StoryObj<typeof WatchCtaCard>;
export const Default: Story = { args: { story: heroStory } };
export const NoWatch: Story = { args: { story: { ...heroStory, hasWatch: false } } };
```

- [ ] **Step 6: ReaderSettingsSheet story**

```tsx
// stories/reader/ReaderSettingsSheet.stories.tsx
import type { Meta, StoryObj } from '@storybook/nextjs-vite';
import { ReaderSettingsSheet } from '@/components/reader/ReaderSettingsSheet';
import { DEFAULT_SETTINGS } from '@/lib/reader/useReaderSettings';

const meta: Meta<typeof ReaderSettingsSheet> = {
  title: 'Reader/ReaderSettingsSheet',
  component: ReaderSettingsSheet,
};
export default meta;

type Story = StoryObj<typeof ReaderSettingsSheet>;
export const Open: Story = {
  args: {
    open: true,
    settings: DEFAULT_SETTINGS,
    onChange: () => {},
    onClose: () => {},
  },
};
export const LargeSize: Story = {
  args: {
    open: true,
    settings: { ...DEFAULT_SETTINGS, fontSize: 21, theme: 'sepia' },
    onChange: () => {},
    onClose: () => {},
  },
};
```

- [ ] **Step 7: Запустить storybook smoke-build**

Run: `pnpm build-storybook`
Expected: success.

- [ ] **Step 8: Запустить storybook test runner**

Run: `pnpm test:storybook`
Expected: PASS.

- [ ] **Step 9: Commit**

```bash
git add stories/
git commit -m "feat(M1): storybook stories for feed/story/reader components"
```

---

## Task 12: Responsive QA + manual verification (M1-06)

**Files:** none (manual + screenshots).

**Контекст:** spec требует визуально верифицировать на 320 / 375 / 414 / 768 / 1024 / 1280 / 1920. Делаем с playwright (уже в devDeps).

- [ ] **Step 1: Запустить dev-сервер**

Run: `pnpm dev`
Expected: server up at http://localhost:3000.

- [ ] **Step 2: Создать responsive smoke script**

```ts
// scripts/responsive-smoke.ts
import { chromium, devices } from 'playwright';
import { mkdir } from 'node:fs/promises';
import { resolve } from 'node:path';

const widths = [320, 375, 414, 768, 1024, 1280, 1920];
const routes = ['/', '/story/hero-1', '/reader/hero-1/7'];

async function main() {
  const outDir = resolve(__dirname, '../tmp/responsive');
  await mkdir(outDir, { recursive: true });
  const browser = await chromium.launch();
  for (const w of widths) {
    const ctx = await browser.newContext({ viewport: { width: w, height: 800 } });
    const page = await ctx.newPage();
    for (const r of routes) {
      const url = `http://localhost:3000${r}`;
      await page.goto(url, { waitUntil: 'domcontentloaded' });
      await page.waitForTimeout(800);
      const slug = r.replace(/\//g, '_').replace(/^_/, '');
      await page.screenshot({
        path: resolve(outDir, `${w}-${slug || 'home'}.png`),
        fullPage: true,
      });
    }
    await ctx.close();
  }
  await browser.close();
  console.log(`✓ screenshots saved to ${outDir}`);
}

main();
```

Add npm script:

```json
// package.json — добавить в "scripts":
"qa:responsive": "tsx --env-file=.env scripts/responsive-smoke.ts"
```

- [ ] **Step 3: Запустить смоук**

Run: `pnpm qa:responsive`
Expected: успешный пробег по 7 viewport × 3 route = 21 скриншот в `tmp/responsive/`.

- [ ] **Step 4: Визуальный обход** (вручную):
  - Открыть `tmp/responsive/`
  - Проверить, что на 320px ничего не overflow'ит
  - На 768-1023 — должен переключаться на mobile layout (по spec'у)
  - На 1280+ — full desktop hero 2-col
  - Документировать видимые проблемы в `docs/qa-notes.md` (если есть)

- [ ] **Step 5: Добавить tmp/ в .gitignore (если ещё нет)**

```
# .gitignore
tmp/
```

- [ ] **Step 6: Commit**

```bash
git add scripts/responsive-smoke.ts package.json .gitignore
git commit -m "chore(M1-06): playwright responsive smoke script (320–1920)"
```

---

## Task 13: Финальный pass + tag

**Files:** none.

- [ ] **Step 1: Запустить полный pipeline**

Run: `pnpm test && pnpm typecheck && pnpm lint && pnpm format:check && pnpm build`
Expected: all PASS, build succeeds.

- [ ] **Step 2: Открыть `/`, `/story/hero-1`, `/reader/hero-1/7` в браузере**

Run: `pnpm dev`
Открыть три URL. Глазами проверить:

- Feed: Marquee бежит, hero виден, grid скроллится, tab bar внизу на mobile
- Story: meta-panel амбер-soft, chapter list 14 пунктов, ch.7 «продолжить» амбер
- Reader: drop cap большой амбер-italic, текст justified, settings sheet открывается, шрифт меняется

- [ ] **Step 3: Tag**

```bash
git tag m1-read
git push --tags
```

(Tag-only без push'a remote если remote не настроен — `git tag m1-read` достаточно.)

---

## Self-Review (выполнено перед сохранением)

**Spec coverage:**

- M1-01 Feed Desktop → Task 3 ✓
- M1-02 Feed Mobile → Task 4 ✓
- M1-03 Story page → Task 5 ✓
- M1-04 Reader → Tasks 6-9 (settings hook, page, progress hook, dim hook) ✓
- M1-05 Tracking → Tasks 2 + 10 ✓
- M1-06 Responsive QA → Task 12 ✓

Из spec'ов screen-files прокрыты: hero, маркиза, fandom chips, tab bar, story chapter list, watch CTA, reader drop cap, settings sheet, late-night dim, reading progress milestones. **НЕ прокрыто (откладывается в backlog или M2):**

- streaming Reader text — M2 (явно в spec'е)
- pull-to-refresh — backlog (S, не критичен для M1)
- IntersectionObserver lazy chunks по 8 в FeedGrid — backlog
- Late-night dim уход в `bg-deepest` — добавить токен или fallback на `bg-black`
- Sepia/true-dark темы visual-toggle — Task 7 переключает значение, но визуально только late-night отрисован (CSS вариации добавить позже, когда токены)

**Placeholder scan:** проверено — все шаги содержат реальный код. «TBD»/«implement later» нет.

**Type consistency:** `Story`, `Chapter`, `ChapterContent`, `ReaderSettings`, `FandomChip` — определены в Task 1 и 6, используются последовательно. `track()` сигнатура `(name, props?)` — везде одинаковая.
